# Required: absolute path on the Docker host to the video directory.
VIDEO_ROOT=/absolute/path/to/videos

# Required before first run: create the writable cuts folder on the host.
# Example: mkdir -p "$VIDEO_ROOT/Cuts"

# Optional: loopback port used to open the application.
WEBAPP_PORT=8080
